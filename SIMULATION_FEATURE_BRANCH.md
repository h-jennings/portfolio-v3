# Long-running feature branch (simulation)

This file exists only to give `hunter/feat/updates` a real diff from `main` so it
behaves like a genuine long-running feature branch in Graphite. Safe to delete.
