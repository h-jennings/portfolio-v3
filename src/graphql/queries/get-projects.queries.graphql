query GetProjects($count: Int = 25) {
  projects(first: $count) {
    id
    name
    featured
    slug
    category
    description {
      raw
    }
    featureMediaWide {
      url
      mediaType
    }
    featureMediaNarrow {
      url
      mediaType
    }
  }
}
